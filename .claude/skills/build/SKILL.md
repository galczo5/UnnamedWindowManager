---
name: build
description: Build the app. Use when the user asks to "build", "compile", "build the app", or wants to check if the project compiles.
---

Run the build script from the project root:

```bash
./build.sh
```

- Use the Bash tool to execute `./build.sh` from `/Users/kamil/Dev/UnnamedWindowManager`
- Report build success or surface any compiler errors to the user
